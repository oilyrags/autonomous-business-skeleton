"""Shared event/data schemas for the walking skeleton.

Pydantic models matching ``architecture/events.asyncapi.yaml``. This is the one
package every service depends on; it carries no business logic.
"""

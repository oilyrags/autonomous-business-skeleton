"""identity service — issues, validates, and revokes short-lived agent JWTs.

Interim custom issuer per ADR-0003 (Keycloak/SPIFFE deferred). Placeholder for
slice 00; implemented in slice 01 (auth) and slice 03 (revocation).
"""

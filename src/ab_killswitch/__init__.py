"""killswitch service — the launch-blocker control.

Holds control flags (global / per-agent), drives token revocation via identity,
and publishes KillSwitchActivated. Placeholder for slice 00; implemented in
slice 04.
"""
